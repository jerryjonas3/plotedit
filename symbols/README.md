# Symbols

Instrument symbols drawn to USITT RP-2. Not the designer's Field Template /
SoftSymbols set — those are .vwx and cannot be reused.

First pass lives in server/plotedit/scaled_pdf.py. Needs marking up before more
are drawn.
